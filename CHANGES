- CHANGES ON THIS VERSION:


-- [FIX] Fixed PBR showing Tip message in MSFS2020 "Recording push back..." while using Manual Push back and Auto-Push back feature

-- [FIX] Record Push Back being not able to record 

-- [FIX] Auto-Push back GUI not working after recording (There is no need to reset the PBR App to use any other feature)

-- [FIX] Push back tug releases the plane without slowing down first (It will now slowdown the plane and then release the nose)

-- [FIX] Added compiled EXE AV whitelisted (You can uninstall Python 3.8.7 from yout system if you whish)

-- [IMPROVE] Improved GUI performance

-- [ADD] Added new app icon

-- [ADD] Added GUI Menu bar

-- [ADD] Tooltips can now be disabled (Check for settings on the UI menu bar)
